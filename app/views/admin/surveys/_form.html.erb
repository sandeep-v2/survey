<%= form_for(@admin_survey) do |f| %>
  <% if @admin_survey.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_survey.errors.count, "error") %> prohibited this admin_survey from being saved:</h2>

      <ul>
      <% @admin_survey.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
